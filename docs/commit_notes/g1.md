# G1

What changed
- Added theme JSON files for default and retro styles.
- Implemented a theme loader and applied theme colors to the playfield, entities, trail, and HUD.

Manual test steps
- Run `python -m air_hockey.main`.
- Click Play and verify the default theme colors render.
- Temporarily set the theme name in `ThemeManager` to `retro` and verify colors change.

Known issues
- Theme selection is hard-coded for now.
