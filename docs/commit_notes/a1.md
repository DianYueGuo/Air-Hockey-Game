# A1

What changed
- Added `pyproject.toml` with runtime and dev dependencies.
- Added initial package layout with a runnable Pygame window stub.
- Recorded initial design decisions for goals and player halves.

Manual test steps
- Create a virtual environment and install dependencies.
- Run `python -m air_hockey.main`.
- Verify a window opens and closes cleanly.

Known issues
- None.
