# App1

What changed
- App now reads persisted window settings on startup.

Manual test steps
- Set fullscreen/display index in Settings and exit.
- Restart the app and verify the window respects the stored settings.

Known issues
- Fullscreen/display changes still require restart.
