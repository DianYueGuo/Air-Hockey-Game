# A2

What changed
- Added a minimal screen manager and app loop abstraction.
- Wired `python -m air_hockey.main` to run the new app wrapper.
- Created UI package placeholders.

Manual test steps
- Run `python -m air_hockey.main`.
- Verify a window opens and closes cleanly.

Known issues
- Screens are placeholders; no UI yet.
