# D1

What changed
- Added a camera capture thread that stores the latest frame.
- Play screen starts the camera on entry and stops it when returning to menu.

Manual test steps
- Run `python -m air_hockey.main`.
- Click Play; verify the app still runs even if no webcam is available.
- Press Escape to return to the menu.

Known issues
- No webcam overlay yet; frames are captured but not displayed.
