# Phys9

What changed
- Set default puck damping to 0.0 for maximum glide.

Manual test steps
- Run `python3 -m air_hockey.main`.
- Strike the puck and confirm it slides without slowing quickly.

Known issues
- Existing settings may override defaults; set Puck Damping to 0.0 in Physics Tuning if needed.
