# B3

What changed
- Added goal detection, scoring, and position reset on goals.
- Rendered a basic score HUD at the top of the play screen.
- Added simple goal markers at the left/right edges.

Manual test steps
- Run `python -m air_hockey.main`.
- Click Play and push the puck into left/right goals.
- Verify the score increments and the puck/mallets reset.

Known issues
- Goal width is a fixed fraction of table height.
