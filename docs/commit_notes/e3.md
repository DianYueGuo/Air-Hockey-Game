# E3

What changed
- Added calibration debug overlays showing captured min/max values per player.

Manual test steps
- Run `python -m air_hockey.main`.
- Click Calibration and capture a few steps.
- Verify the debug lines update with the captured values.

Known issues
- Debug values are raw camera pixels and may be noisy until calibrated.
