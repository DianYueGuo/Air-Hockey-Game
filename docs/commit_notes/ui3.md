# UI3

What changed
- Menu now uses the resolved window size for centering calculations.

Manual test steps
- Run `python3 -m air_hockey.main`.
- Verify the main menu is centered correctly, especially in fullscreen.

Known issues
- None.
