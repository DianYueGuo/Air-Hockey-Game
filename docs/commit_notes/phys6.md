# Phys6

What changed
- Reduced default puck damping to allow longer sliding after hits.

Manual test steps
- Run `python3 -m air_hockey.main`.
- In Play, strike the puck and confirm it glides longer.

Known issues
- Existing settings may override defaults; adjust Puck Damping in Physics Tuning if needed.
